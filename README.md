# Squared Examples

Independent, complete applications built with the Squared framework and
Squared Project Generator. These projects are deliberately kept outside the
framework and generator repositories s experiments cannot change their
release code.

## Applications

currently empty!!

squared-pg is undergoing a massive rewrite and will be
unavailable. Thus examples will have to wait.

Build it in Termux:

```sh


```

The APK is written to:

```text

```

## Repository model

